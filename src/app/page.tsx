import Image from 'next/image';

export default function Home() {
  return (
    <main className='flex min-h-screen flex-col items-center justify-between p-24'>
      <div>
        just changing the page.tsx to see if I will be able to deploy it to
        vercel
      </div>
    </main>
  );
}
